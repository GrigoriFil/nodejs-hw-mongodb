# swagger/components/responses/200-ContactFound.yaml

description: Contact details retrieved successfully
content:
  application/json:
    schema:
      type: object
      required: [status, message, data]
      properties:
        status:
          type: integer
          example: 200
        message:
          type: string
          example: "Successfully found contact!"
        data:
          $ref: '../schemas/Contact.yaml'